# Excluded files

The uploaded archive also contained files that were intentionally omitted from the public portfolio wrapper:

- assignment specification PDFs;
- LTspice tutorial/reference PDFs;
- PDFs belonging to other student IDs;
- a duplicate copy of the third submission PDF;
- `.DS_Store` and `__MACOSX` metadata.

These exclusions avoid publishing course handouts, duplicate data, and work that is not the portfolio owner's authorship.
